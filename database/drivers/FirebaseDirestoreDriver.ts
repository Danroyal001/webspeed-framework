import DatabaseDriver from '../DatabaseDriver';

class FirebaseFirestoreDriver extends DatabaseDriver {
    constructor() {
        super();
    }

    read() {
        // 
    }

    write() {
        // 
    }
}

export default FirebaseFirestoreDriver;
